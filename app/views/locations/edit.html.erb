<% provide(:title, 'Edit Location') %>
<% provide(:header_current, 'Setup') %>
<% provide(:sidebar_current, 'Locations') %>

<div class="page-header">
	<h1><%= yield(:title) %></h1>
</div>

<%= form_for @location, :builder => BootstrapFormBuilder, :html => { :class => 'form-horizontal' } do |f| %>
	<%= f.error_messages %>
	<%= render 'form', :f => f %>
	
	<%= f.submit_group do %>
		<%= f.submit_primary 'Update' %>
	<% end %>
<% end %>
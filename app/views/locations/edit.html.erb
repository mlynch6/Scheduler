<% provide(:title, 'Edit Location') %>
<% provide(:header_current, 'Administration') %>
<% provide(:current, 'EditLocation') %>

<h1>Edit Location</h1>

<div class="span9">
	<%= form_for @location, :html => { :class => 'form-horizontal' } do |f| %>
		<%= render 'shared/error_messages', :object => f.object %>
		<%= render 'form', :f => f %>
		
		<%= render 'shared/form_submit', :f => f, :action_text => 'Update' %>
	<% end %>
</div>

<div class="span3">
	<%= render 'shared/subnav' %>
</div>
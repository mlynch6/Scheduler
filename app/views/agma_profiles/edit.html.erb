<% provide(:title, 'Edit Rehearsal Week Settings') %>
<% provide(:header_current, 'Administration') %>
<% provide(:current, 'EditRehearsal') %>

<h1>Edit Rehearsal Week Settings</h1>

<div class="span9">
	<%= form_for @agma_profile, :html => { :class => 'form-horizontal' } do |f| %>
		<%= render 'shared/error_messages', :object => f.object %>
		
		<%= render 'form', :f => f %>
		<%= render 'shared/form_submit', :f => f, :action_text => 'Update' %>
	<% end %>
</div>

<div class="span3">
	<%= render 'shared/subnav' %>
</div>
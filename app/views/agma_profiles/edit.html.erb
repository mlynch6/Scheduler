<% provide(:title, 'Edit Rehearsal Week Settings') %>
<% provide(:header_current, 'Administration') %>

<h1>Edit Rehearsal Week Settings</h1>

<div class="row">
	<%= form_for @agma_profile, :builder => BootstrapFormBuilder, :html => { :class => 'form-horizontal' } do |f| %>
		<%= f.error_messages %>
		<%= render 'form', :f => f %>
		<%= f.submit 'Update', :class => 'btn btn-primary' %>
	<% end %>
</div>
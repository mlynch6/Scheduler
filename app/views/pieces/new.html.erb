<% provide(:title, 'Add Piece') %>
<% provide(:header_current, 'Scheduling') %>

<h1>Add Piece</h1>

<div class="span9">
	<%= form_for @piece, :builder => BootstrapFormBuilder, :html => { :class => 'form-horizontal' } do |f| %>
		<%= f.error_messages %>
		<%= render 'form', :f => f %>
		<%= f.submit 'Create', :class => 'btn btn-primary' %>
	<% end %>
</div>

<div class="span3">
	<div class="alert alert-info"><i class="icon-info-sign"></i> 
		Add Scenes and Roles to your piece now if you know them. You can always add or update these later.
	</div>
</div>
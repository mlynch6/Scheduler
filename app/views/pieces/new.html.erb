<% provide(:title, 'Add Piece') %>
<% provide(:header_current, 'Setup') %>
<% provide(:sidebar_current, 'Pieces') %>

<div class="row">
  <div class="page-header col-md-11">
  	 <h1>
      Pieces
      <small>Add</small>
    </h1>
  </div>
  <%= render 'pieces/tools_menu' %>
</div>

<div class="row">
  <%= form_for @piece, :builder => BootstrapFormBuilder, :html => { :class => 'form-horizontal' } do |f| %>
  	<%= f.error_messages %>
  	<%= render 'form', :f => f %>
	
  	<%= f.submit_group do %>
  		<%= f.submit_primary 'Create' %>
      <%= link_to 'Cancel', pieces_path, class: 'btn btn-link' %>
  	<% end %>
  <% end %>
</div>
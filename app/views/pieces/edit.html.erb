<% provide(:title, 'Edit Piece') %>
<% provide(:header_current, 'Scheduling') %>
<% provide(:current, 'EditPiece') %>

<h1>Edit Piece</h1>

<div class="span9">
	<%= form_for @piece, :html => { :class => 'form-horizontal' } do |f| %>
		<%= render 'shared/error_messages', :object => f.object %>
		<%= render 'form', :f => f %>
		
		<%= render 'shared/form_submit', :f => f, :action_text => 'Update' %>
	<% end %>
</div>

<div class="span3">
	<%= render 'shared/subnav' %>
</div>
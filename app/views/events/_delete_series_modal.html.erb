<div class="modal fade" id="deleteModal" tabindex="-1" role="dialog" aria-labelledby="deleteModalLabel" aria-hidden="true">
<div class="modal-dialog">
<div class="modal-content">

	<div class="modal-header">
		<button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
		<h3 class="modal-title" id="deleteModalLabel">
			<%= content_tag(:span, nil, class: "glyphicon glyphicon-calendar") %> You are deleting an event
		</h3>
	</div>

	<div class="modal-body">
		Do you want to delete this and all future occurrences of this event, or only the selected occurrence?
	</div>

	<div class="modal-footer">
		<button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
		&nbsp;
		&nbsp;
		
		<% if event == event.event_series.events.first %>
			<%= delete_button 'All', event_path(:id => event.id, :mode => "all"), class: 'mash-no-confirm' %>
		<% else %>
			<%= delete_button 'All Future Events', event_path(:id => event.id, :mode => "future"), class: 'mash-no-confirm' %>
		<% end %>
		
		<%= delete_button 'Only This Event', event, class: 'mash-no-confirm' %>
	</div>
	
</div> <!-- modal-content -->
</div> <!-- modal-dialog -->
</div> <!-- modal -->
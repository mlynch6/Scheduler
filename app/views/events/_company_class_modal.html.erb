<div class="modal fade" id="modal_<%= appt.id %>" tabindex="-1" role="dialog" aria-labelledby="modalLabel_<%= appt.id %>" aria-hidden="true">
<div class="modal-dialog">
<div class="modal-content">

	<div class="modal-header">
		<button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
		<h3 class="modal-title" id="modalLabel_<%= appt.id %>">
			<%= appt.title %>
			<small>Company Class</small>
		</h3>
	</div>
	
	<div class="modal-body">
		<%= render 'company_classes/detail', cclass: appt %>
	</div>
	
	<div class="modal-footer">
		<% if allow? "company_classes", "edit" %>
			<%= edit_button "Edit", edit_company_class_path(appt) %>
		<% end %>
	</div>

</div> <!-- modal-content -->
</div> <!-- modal-dialog -->
</div> <!-- modal -->
<% provide(:title, 'Edit Company Class') %>
<% provide(:header_current, 'Scheduling') %>
<% provide(:current, 'EditCompanyClass') %>

<h1>Edit Company Class</h1>

<div class="span9">
	<%= form_for @cclass, builder: BootstrapFormBuilder, :html => { :class => 'form-horizontal' } do |f| %>
		<%= f.error_messages %>
		<%= render 'form', :f => f %>
		<%= f.submit 'Update', :class => 'btn btn-primary' %>
	<% end %>
</div>

<div class="span3">	
	<%= render 'shared/subnav' %>
</div>
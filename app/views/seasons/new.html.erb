<% provide(:title, 'Add Season') %>
<% provide(:header_current, 'Setup') %>
<% provide(:sidebar_current, 'Seasons') %>

<div class="row">
  <div class="page-header col-md-11">
  	 <h1>
      Seasons
      <small>Add</small>
    </h1>
  </div>
  <%= render 'seasons/tools_menu' %>
</div>

<div class="row">
  <%= form_for @season, :builder => BootstrapFormBuilder, :html => { :class => 'form-horizontal' } do |f| %>
  	<%= f.error_messages %>
  	<%= render 'form', :f => f %>
	
  	<%= f.submit_group do %>
  		<%= f.submit_primary 'Create' %>
      <%= link_to 'Cancel', seasons_path, class: 'btn btn-link' %>
  	<% end %>
  <% end %>
</div>
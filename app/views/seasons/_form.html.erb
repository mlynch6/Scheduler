<%= f.input :name %>
<%= f.input :start_dt, as: :date_picker, :label => 'From' %>
<%= f.input :end_dt, as: :date_picker, :label => 'To' %>
<%= f.association :pieces, as: :chosen_select, 
      collection: @pieces %>
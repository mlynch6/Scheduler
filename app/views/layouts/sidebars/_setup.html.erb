<% klass = ' class="active"'.html_safe %>
<nav class="navbar navbar-default mash-sub-navbar" role="navigation">
  <ul class="nav navbar-nav">
		<% if can? :read, Location %>
			<li<%= klass if yield(:sidebar_current) == "Locations" %>>
				<%= link_to "Locations", locations_path(status: "active") %>
			</li>
		<% end %>
    
		<% if can? :read, :contract_rehearsal_week %>
			<li<%= klass if yield(:sidebar_current) == "Contract" %>>
				<%= link_to "Contract Settings", company_contract_rehearsal_week_path %>
			</li>
		<% end %>
				
		<% if can? :read, Season %>
			<li<%= klass if yield(:sidebar_current) == "Seasons" %>>
				<%= link_to "Seasons", seasons_path %>
			</li>
		<% end %>
		
		<% if can? :read, Piece %>
			<li<%= klass if yield(:sidebar_current) == "Pieces" %>>
				<%= link_to "Pieces", pieces_path %>
			</li>
		<% end %>
  </ul>
</nav>
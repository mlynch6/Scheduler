<% klass = ' class="active"'.html_safe %>
<nav class="navbar navbar-default mash-sub-navbar" role="navigation">
  <ul class="nav navbar-nav">
		<% if allow? "admin_accounts", "index" %>
			<li<%= klass if yield(:sidebar_current) == "Accounts" %>>
        <%= link_to "Accounts", admin_accounts_path %>
      </li>
		<% end %>
    
		<% if allow? "subscription_plans", "index" %>
			<li<%= klass if yield(:sidebar_current) == "Subscription Plans" %>>
        <%= link_to "Subscription Plans", admin_subscription_plans_path %>
      </li>
		<% end %>
    
		<% if allow? "dropdowns", "index" %>
			<li<%= klass if yield(:sidebar_current) == "Dropdowns" %>>
        <%= link_to "Dropdowns", admin_dropdowns_path %>
      </li>
		<% end %>
  </ul>
</nav>
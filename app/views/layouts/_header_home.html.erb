<% class_active = ' class=active' %>
<ul class="nav navbar-nav">
  <% if can? :home, :static_page %>
  	<li<%= class_active if yield(:header_current) == "GuestHome" %>>
      <%= link_to "Home", root_path %>
    </li>
  <% end %>
  
  <% if can? :features, :static_page %>
  	<li<%= class_active if yield(:header_current) == "Features" %>>
      <%= link_to "Features", features_path %>
    </li>
  <% end %>
	
  <% if can? :pricing, :static_page %>
    <li<%= class_active if yield(:header_current) == "Pricing" %>>
      <%= link_to "Pricing & Signup", pricing_path %>
    </li>
  <% end %>
	
  <% if can? :contact, :static_page %>
    <li<%= class_active if yield(:header_current) == "Contact" %>>
      <%= link_to "Contact Us", contact_path %>
    </li>
  <% end %>
</ul>

<ul class="nav navbar-nav navbar-right">
	<li class="divider-vertical"></li>
  <% if can? :create, :session %>
	  <li><%= link_to "Sign In", login_path %></li>
  <% end %>
</ul>
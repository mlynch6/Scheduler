<% klass = ' class="active"'.html_safe %>
<ul class="nav nav-tabs">
  <% if can? :read, @costume_fitting %>
  	<li<%= klass if %w[show edit update].include?(params[:action]) %>>
      <%= icon_link 'Overview', 'asterisk', schedule_costume_fitting_path(@costume_fitting) %>
    </li>
  <% end %>
  <% if can? :invitees, @costume_fitting %>
  	<li<%= klass if %w[invitees edit_invitees update_invitees].include?(params[:action]) %>>
      <%= icon_link 'Invitees', 'user', invitees_schedule_costume_fitting_path(@costume_fitting) %>
    </li>
  <% end %>
</ul>
<% if @scenes.count == 0 %>
  $("div.rehearsal_scene_id").hide()
<% else %>
  $("div.rehearsal_scene_id").show()
<% end %>
$("#rehearsal_scene_id").html('<option value=""></option><%= escape_javascript(options_for_select(@scenes.map {|sc| [sc.name, sc.id]})) %>')
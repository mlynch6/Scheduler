<% provide(:title, "#{@piece.name} | Add Character") %>
<% provide(:header_current, 'Scheduling') %>
<% provide(:current, 'AddCharacter') %>

<h1>Add Character for <%= @piece.name %></h1>

<div class="span9">
	<%= form_for [@piece, @character], :builder => BootstrapFormBuilder, :html => { :class => 'form-horizontal' } do |f| %>
		<%= f.error_messages %>
		<%= render 'form', :f => f %>
		<%= f.submit 'Create', :class => 'btn btn-primary' %>
	<% end %>
</div>

<div class="span3">
	<%= render 'shared/subnav' %>
</div>
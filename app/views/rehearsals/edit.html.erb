<% provide(:title, 'Edit Rehearsal') %>
<% provide(:header_current, 'Scheduling') %>
<% provide(:current, 'EditRehearsal') %>

<h1>Edit Rehearsal</h1>

<div class="span9">
	<%= form_for @rehearsal, :html => { :class => 'form-horizontal' } do |f| %>
		<%= render 'shared/error_messages', :object => f.object %>
		
		<%= render 'form', :f => f %>
		<%= render 'shared/form_submit', :f => f, :action_text => 'Update' %>
	<% end %>
</div>

<div class="span3">	
	<%= render 'shared/subnav' %>
</div>
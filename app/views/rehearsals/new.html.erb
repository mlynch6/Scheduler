<% provide(:title, 'New Rehearsal') %>
<% provide(:header_current, 'Calendar') %>
<% provide(:sidebar_current, 'New Rehearsal') %>

<div class="page-header">
	<h1><%= yield(:title) %></h1>
</div>

<%= form_for @rehearsal, builder: BootstrapFormBuilder, :html => { :class => 'form-horizontal' } do |f| %>
	<%= f.error_messages %>
	<%= render 'form', :f => f %>
	
	<%= f.submit_group do %>
		<%= f.submit_primary 'Create' %>
	<% end %>
<% end %>